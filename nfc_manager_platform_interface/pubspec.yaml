name: nfc_manager_platform_interface
description: A common platform interface for the nfc_manager plugin.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  test_cov_console: ^0.2.2

  # Linter
  cosee_lints: ^0.6.0
  dart_code_linter: ^1.2.1
